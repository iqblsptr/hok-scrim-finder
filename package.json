{
  "name": "hok-scrim-finder",
  "version": "1.0.0",
  "type": "module",  
  "scripts": {
    "start": "...",
    "build": "echo 'Building static files and API...' && npm install" 
  },
  "dependencies": {
    "@vercel/postgres": "^0.5.1",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.3.1",
    "@vercel/blob": "^0.15.0"
  }
}